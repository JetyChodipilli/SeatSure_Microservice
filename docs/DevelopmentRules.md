# Development Rules

- Work only on develop branch
- One service at a time
- Every service must compile
- Every sprint ends with Git commit
- Never push broken code
- Every API must be tested
- Every feature must be documented